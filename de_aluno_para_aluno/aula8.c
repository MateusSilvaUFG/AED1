#include <stdio.h>

int main ()
{
    int inteira = 10; //inteiro
    float decimal = 10.5; //para decimal
    char letra = 'a'; //para texto.

    printf("inteiro = %i, decimal = %f, letra = %c.\n", inteira, decimal, letra);

    return 0;
}